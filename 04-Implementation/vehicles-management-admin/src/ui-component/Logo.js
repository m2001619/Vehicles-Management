// ==============================|| LOGO ||============================== //

import { defaultContent } from '../constans/constans';

const Logo = () => {
  return <img src={defaultContent.adminLogo} alt={defaultContent.adminTitle} width="60px" height="60px" />;
};

export default Logo;
